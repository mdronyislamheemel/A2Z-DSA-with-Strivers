{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compile and run with input/output",
      "type": "shell",
      "command": "g++ -std=c++17 -o main \"${file}\" && main < input.txt > output.txt",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}
